network-compression-threshold:
  - expressions:
    - server_properties["network-compression-threshold"] <= 256
    - spigot["settings"]["bungeecord"] == "false"
    prefix: "❌"
    value: "在 server.properties 中增加该值。推荐值：512。"
